-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 27-09-2025 22:41
module( ..., package.seeall )
function WatchRankT9ProgressPip( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 38 * _1080p, 0, 38 * _1080p )
	self.id = "WatchRankT9ProgressPip"
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine.DDJFBBJAIG() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1 )
	local f1_local2 = self
	local PipShadow = nil
	
	PipShadow = LUI.UIImage.new()
	PipShadow.id = "PipShadow"
	PipShadow:SetRGBFromInt( 0, 0 )
	self:addElement( PipShadow )
	self.PipShadow = PipShadow
	
	local Pip = nil
	
	Pip = LUI.UIImage.new()
	Pip.id = "Pip"
	Pip:SetRGBFromInt( 0, 0 )
	Pip:SetAlpha( 0.2, 0 )
	Pip:SetAnchorsAndPosition( 0, 0, 0, 0, 0, 0, _1080p * 4, _1080p * 4 )
	self:addElement( Pip )
	self.Pip = Pip
	
	return self
end

MenuBuilder.registerType( "WatchRankT9ProgressPip", WatchRankT9ProgressPip )
LockTable( _M )
