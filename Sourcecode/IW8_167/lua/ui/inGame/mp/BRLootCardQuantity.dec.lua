-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 13-07-2025 21:52
module( ..., package.seeall )
function BRLootCardQuantity( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 130 * _1080p, 0, 30 * _1080p )
	self.id = "BRLootCardQuantity"
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine.DDJFBBJAIG() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1 )
	local f1_local2 = self
	local Bg = nil
	
	Bg = LUI.UIImage.new()
	Bg.id = "Bg"
	Bg:SetRGBFromTable( SWATCHES.genericMenu.blackBackground, 0 )
	Bg:SetAlpha( 0, 0 )
	self:addElement( Bg )
	self.Bg = Bg
	
	local Text = nil
	
	Text = LUI.UIStyledText.new()
	Text.id = "Text"
	Text:setText( "", 0 )
	Text:SetFont( FONTS.GetFont( FONTS.MainBold.File ) )
	Text:SetWordWrap( false )
	Text:SetAlignment( LUI.Alignment.Center )
	self:addElement( Text )
	self.Text = Text
	
	return self
end

MenuBuilder.registerType( "BRLootCardQuantity", BRLootCardQuantity )
LockTable( _M )
