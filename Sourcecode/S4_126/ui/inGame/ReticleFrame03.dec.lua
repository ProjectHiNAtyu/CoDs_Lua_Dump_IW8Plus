-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 04-10-2025 21:42
module( ..., package.seeall )
local f0_local0 = function ( f2_arg0, f2_arg1, f2_arg2 )
	f2_arg0:ReceivePostFX()
end

function ReticleFrame03( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 30 * _1080p, 0, 20 * _1080p )
	self.id = "ReticleFrame03"
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine[0x12586A3F4FB490E]() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1, "Missing controllerIndex!" )
	local f1_local2 = self
	local MeterLeft = nil
	
	MeterLeft = LUI.UIImage.new()
	MeterLeft.id = "MeterLeft"
	MeterLeft:SetRGBFromTable( SWATCHES.Reticles.Black, 0 )
	MeterLeft:SetAlpha( 0.8, 0 )
	MeterLeft:SetZRotation( -90, 0 )
	MeterLeft:setImage( RegisterMaterial( "hud_reticle_notched_meter_03" ), 0 )
	MeterLeft:SetUMin( -0.01, 0 )
	MeterLeft:SetUMax( 1.01, 0 )
	MeterLeft:SetAnchorsAndPosition( 0, 1, 0, 0, _1080p * -118, 0, _1080p * 5, _1080p * -5 )
	self:addElement( MeterLeft )
	self.MeterLeft = MeterLeft
	
	local MeterRight = nil
	
	MeterRight = LUI.UIImage.new()
	MeterRight.id = "MeterRight"
	MeterRight:SetRGBFromTable( SWATCHES.Reticles.Black, 0 )
	MeterRight:SetAlpha( 0.8, 0 )
	MeterRight:SetZRotation( 90, 0 )
	MeterRight:setImage( RegisterMaterial( "hud_reticle_notched_meter_03" ), 0 )
	MeterRight:SetUMin( -0.01, 0 )
	MeterRight:SetUMax( 1.01, 0 )
	MeterRight:SetAnchorsAndPosition( 1, 0, 0, 0, 0, _1080p * 118, _1080p * 5, _1080p * -5 )
	self:addElement( MeterRight )
	self.MeterRight = MeterRight
	
	f0_local0( self, f1_local1, controller )
	return self
end

MenuBuilder.registerType( "ReticleFrame03", ReticleFrame03 )
LockTable( _M )
