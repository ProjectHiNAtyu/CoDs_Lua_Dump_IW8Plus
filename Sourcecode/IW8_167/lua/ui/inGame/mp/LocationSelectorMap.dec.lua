-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 13-07-2025 21:52
module( ..., package.seeall )
function PostLoadFunc( f3_arg0, f3_arg1, f3_arg2 )
	if CONDITIONS.IsBRGameType( f3_arg0 ) then
		local f3_local0 = DataSources.inGame.player.BRData.showTacMap
		local f3_local1 = DataSources.inGame.player.BRData.showTacMap:GetValue( f3_arg1 )
	end
	if not (f3_local0 and f3_local1 or false) then
		if MP.IsCurrentMapBigMap() then
			f3_arg0.Map:ChangeMinimapCompassType( CoD.CompassType.Partial )
		end
		f3_arg0.Map:SetIsUsingTabletMode( true )
	end
end

function LocationSelectorMap( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 1024 * _1080p, 0, 1024 * _1080p )
	self.id = "LocationSelectorMap"
	self._animationSets = self._animationSets or {}
	self._sequences = self._sequences or {}
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine.DDJFBBJAIG() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1 )
	local f1_local2 = self
	local Map = nil
	
	Map = MenuBuilder.BuildRegisteredType( "MinimapContent", {
		boundsRadius = 0,
		mapMask = "stencil_mask",
		drawLocationSelectionCursor = true,
		mapAlpha = 1,
		compassType = CoD.CompassType.Full,
		controllerIndex = f1_local1
	} )
	Map.id = "Map"
	self:addElement( Map )
	self.Map = Map
	
	local Border = nil
	
	Border = LUI.UIBorder.new( {
		borderThicknessLeft = _1080p * 1,
		borderThicknessRight = _1080p * 1,
		borderThicknessTop = _1080p * 1,
		borderThicknessBottom = _1080p * 1
	} )
	Border.id = "Border"
	Border:SetRGBFromTable( SWATCHES.StreakTablet.OSPrimary, 0 )
	Border:SetAlpha( 0.4, 0 )
	self:addElement( Border )
	self.Border = Border
	
	Map:RegisterAnimationSequence( "DefaultSequence", {
		{
			value = 0,
			duration = 0,
			property = TWEEN_PROPERTY.Alpha
		},
		{
			value = 0,
			duration = 300,
			property = TWEEN_PROPERTY.Alpha
		},
		{
			value = 1,
			duration = 300,
			property = TWEEN_PROPERTY.Alpha,
			easing = LUI.EASING.outBack
		}
	} )
	self._sequences.DefaultSequence = function ()
		menu:AnimateSequence( "DefaultSequence" )
	end
	
	PostLoadFunc( self, f1_local1, controller )
	ACTIONS.AnimateSequence( self, "DefaultSequence" )
	return self
end

MenuBuilder.registerType( "LocationSelectorMap", LocationSelectorMap )
LockTable( _M )
