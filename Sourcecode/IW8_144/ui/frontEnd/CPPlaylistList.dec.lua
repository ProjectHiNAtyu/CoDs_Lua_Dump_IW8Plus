-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 27-09-2025 22:41
module( ..., package.seeall )
function CPPlaylistList( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 442 * _1080p, 0, 690 * _1080p )
	self.id = "CPPlaylistList"
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine.DDJFBBJAIG() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1 )
	local f1_local2 = self
	local Scrollbar = nil
	
	Scrollbar = MenuBuilder.BuildRegisteredType( "VerticalMinimalScrollbar", {
		controllerIndex = f1_local1
	} )
	Scrollbar.id = "Scrollbar"
	Scrollbar:SetAnchorsAndPosition( 0, 1, 0, 1, _1080p * 426, _1080p * 442, 0, _1080p * 690 )
	self:addElement( Scrollbar )
	self.Scrollbar = Scrollbar
	
	local PlaylistButtons = nil
	
	PlaylistButtons = LUI.UIDataSourceGrid.new( {
		scrollingThresholdX = 1,
		buildArrowsLabel = false,
		maxVelocity = 5000,
		adjustSizeToContent = true,
		scrollingThresholdY = 1,
		wrapX = false,
		maxVisibleColumns = 1,
		maxVisibleRows = 14,
		wrapY = true,
		arrowsVerticalOffset = 0,
		springCoefficient = 400,
		controllerIndex = f1_local1,
		buildChild = function ()
			return MenuBuilder.BuildRegisteredType( "PlaylistButton", {
				controllerIndex = menu
			} )
		end,
		primaryAxis = LUI.DIRECTION.horizontal,
		spacingX = _1080p * 10,
		spacingY = _1080p * 10,
		columnWidth = _1080p * 420,
		rowHeight = _1080p * 40,
		horizontalAlignment = LUI.Alignment.Left,
		verticalAlignment = LUI.Alignment.Top
	} )
	PlaylistButtons.id = "PlaylistButtons"
	PlaylistButtons:setUseStencil( true )
	PlaylistButtons:SetAnchorsAndPosition( 0, 1, 0, 1, 0, _1080p * 420, 0, _1080p * 690 )
	self:addElement( PlaylistButtons )
	self.PlaylistButtons = PlaylistButtons
	
	local ArrowUp = nil
	
	ArrowUp = MenuBuilder.BuildRegisteredType( "NavigationArrow", {
		controllerIndex = f1_local1
	} )
	ArrowUp.id = "ArrowUp"
	ArrowUp:SetAnchorsAndPosition( 0.5, 0.5, 1, 0, _1080p * -100, _1080p * -68, _1080p * 14, _1080p * 46 )
	self:addElement( ArrowUp )
	self.ArrowUp = ArrowUp
	
	local ArrowDown = nil
	
	ArrowDown = MenuBuilder.BuildRegisteredType( "NavigationArrow", {
		controllerIndex = f1_local1
	} )
	ArrowDown.id = "ArrowDown"
	ArrowDown.Arrow:SetZRotation( 180, 0 )
	ArrowDown:SetAnchorsAndPosition( 0.5, 0.5, 1, 0, _1080p * 68, _1080p * 100, _1080p * 14, _1080p * 46 )
	self:addElement( ArrowDown )
	self.ArrowDown = ArrowDown
	
	local ListCount = nil
	
	ListCount = LUI.UIText.new()
	ListCount.id = "ListCount"
	ListCount:SetRGBFromTable( SWATCHES.genericMenu.featuredHeader, 0 )
	ListCount:setText( "", 0 )
	ListCount:SetFont( FONTS.GetFont( FONTS.MainRegular.File ) )
	ListCount:SetAlignment( LUI.Alignment.Center )
	ListCount:SetVerticalAlignment( LUI.Alignment.Center )
	ListCount:SetAnchorsAndPosition( 0.5, 0.5, 1, 0, _1080p * -68, _1080p * 68, _1080p * 20, _1080p * 40 )
	self:addElement( ListCount )
	self.ListCount = ListCount
	
	local f1_local8 = LUI.UIScrollbar.new
	local f1_local9 = {
		direction = LUI.DIRECTION.vertical,
		eventListener = Scrollbar,
		startCap = Scrollbar.startCap,
		endCap = Scrollbar.endCap,
		sliderArea = Scrollbar.sliderArea
	}
	local f1_local10 = Scrollbar.sliderArea
	if f1_local10 then
		f1_local10 = Scrollbar.sliderArea.slider
	end
	f1_local9.slider = f1_local10
	f1_local10 = Scrollbar.sliderArea
	if f1_local10 then
		f1_local10 = Scrollbar.sliderArea.fixedSizeSlider
	end
	f1_local9.fixedSizeSlider = f1_local10
	PlaylistButtons:AddScrollbar( f1_local8( f1_local9 ) )
	PlaylistButtons:AddArrow( ArrowUp )
	PlaylistButtons:AddArrow( ArrowDown )
	PlaylistButtons:AddItemNumbers( ListCount )
	return self
end

MenuBuilder.registerType( "CPPlaylistList", CPPlaylistList )
LockTable( _M )
