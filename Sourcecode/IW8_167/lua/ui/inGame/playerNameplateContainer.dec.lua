-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 13-07-2025 21:51
module( ..., package.seeall )
local f0_local0 = {
	NAMEPLATE_MODE_BR = 1,
	NAMEPLATE_MODE_GW = 2,
	NAMEPLATE_MODE_ARENA = 3,
	NAMEPLATE_MODE_DEFAULT = 0
}
function PostLoadFunc( f2_arg0, f2_arg1, f2_arg2 )
	local f2_local0 = {}
	f2_arg0.playerNameplates = LUI.UIElement.new()
	f2_arg0.playerNameplates.id = "playerNameplates"
	if Engine.ECHCFGDDDF() and Game.HJHDJDCBF() and Game.BFEEGCHICA( f2_arg1, "ui_tac_ops_map_open" ) then
		f2_local0.nameplateSetting = StringTable.BJJBBCJGEJ( CSV.nameplateSettings.file, CSV.nameplateSettings.cols.ref, "tac_ops_map" )
		f2_local0.tacOpsMap = true
	elseif CONDITIONS.IsHillMapNameplateOverride() then
		f2_local0.nameplateSetting = StringTable.BJJBBCJGEJ( CSV.nameplateSettings.file, CSV.nameplateSettings.cols.ref, "mp_m_hill" )
	elseif MatchRules.EAIEFBHGJ() then
		f2_local0.nameplateSetting = MatchRules.BIJEDCBCBH( "commonOption", "nameplateSetting" )
	end
	f2_arg0.playerNameplates:SetupPlayerNameplates( f2_local0, f2_arg1, MLG.FBFGBEC() )
	f2_arg0.playerNameplates:ShouldDisplayRank( false )
	if CONDITIONS.IsMagmaGameMode( f2_arg0 ) then
		f2_arg0.playerNameplates:SetGameMode( f0_local0.NAMEPLATE_MODE_BR )
	else
		if CONDITIONS.IsArmsRaceGameType( f2_arg0 ) then
			f2_arg0.playerNameplates:SetGameMode( f0_local0.NAMEPLATE_MODE_GW )
		elseif CONDITIONS.IsArenaGameType( f2_arg0 ) then
			f2_arg0.playerNameplates:SetGameMode( f0_local0.NAMEPLATE_MODE_ARENA )
		end
		if Engine.ECHCFGDDDF() then
			local f2_local1 = LUI.DataSourceFromOmnvar.new( "ui_match_in_progress" )
			f2_arg0:SubscribeToModel( f2_local1:GetModel( f2_arg1 ), function ()
				local f3_local0 = Game.BFEEGCHICA( f2_arg0, "ui_match_in_progress" )
				if f3_local0 ~= nil then
					f2_arg0.playerNameplates:ShouldDisplayChevron( f3_local0 == true )
				end
			end )
		end
	end
	f2_arg0:addElement( f2_arg0.playerNameplates )
end

function playerNameplateContainer( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 276 * _1080p, 0, 50 * _1080p )
	self.id = "playerNameplateContainer"
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine.DDJFBBJAIG() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1 )
	local f1_local2 = self
	PostLoadFunc( self, f1_local1, controller )
	return self
end

MenuBuilder.registerType( "playerNameplateContainer", playerNameplateContainer )
LockTable( _M )
