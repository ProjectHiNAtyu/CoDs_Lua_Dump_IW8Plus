-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 04-10-2025 21:43
module( ..., package.seeall )
require( "ui.inGame.vehicleInstruction" )
local f0_local0 = function ( f3_arg0, f3_arg1, f3_arg2 )
	f3_arg0.InstructionSwitch:UpdateInstruction( Engine[0xED84C33EC5F01EA]( "KILLSTREAKS_HINTS/INSTRUCTION_SWITCH_SEATS" ) )
end

function PlayerNumBox( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 19 * _1080p, 0, 19 * _1080p )
	self.id = "PlayerNumBox"
	self._animationSets = self._animationSets or {}
	self._sequences = self._sequences or {}
	self._sequenceLengths = self._sequenceLengths or {}
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	f1_local1 = LUI.ValidateControllerIndex( self, f1_local1 )
	local f1_local2 = self
	local playerNumberBoxEmpty = nil
	
	playerNumberBoxEmpty = LUI.UIImage.new()
	playerNumberBoxEmpty.id = "playerNumberBoxEmpty"
	playerNumberBoxEmpty:SetRGBFromTable( SWATCHES.highlight.vDefaultHighlight, 0 )
	playerNumberBoxEmpty:SetAlpha( 0.6, 0 )
	playerNumberBoxEmpty:setImage( RegisterMaterial( "hud_jeep_diagram_circle" ), 0 )
	self:addElement( playerNumberBoxEmpty )
	self.playerNumberBoxEmpty = playerNumberBoxEmpty
	
	local playerNumberBox = nil
	
	playerNumberBox = LUI.UIImage.new()
	playerNumberBox.id = "playerNumberBox"
	playerNumberBox:SetRGBFromTable( SWATCHES.StreakTablet.OSSecondary, 0 )
	playerNumberBox:setImage( RegisterMaterial( "hud_jeep_diagram_circle" ), 0 )
	self:addElement( playerNumberBox )
	self.playerNumberBox = playerNumberBox
	
	local playerNumber = nil
	
	playerNumber = LUI.UIText.new()
	playerNumber.id = "playerNumber"
	playerNumber:SetRGBFromTable( SWATCHES.highlight.vOffBlack, 0 )
	playerNumber:setText( "1", 0 )
	playerNumber:SetFont( FONTS.GetFont( FONT_STYLES.fontStyle_XSHeaderBold.font.File ) )
	playerNumber:SetFontSize( FONT_STYLES.fontStyle_XSHeaderBold.size )
	playerNumber:SetMinFontSize( FONT_STYLES.fontStyle_XSHeaderBold.minSize )
	playerNumber:SetWordWrap( true )
	playerNumber:SetAlignment( LUI.Alignment.Center )
	playerNumber:SetVerticalAlignment( LUI.Alignment.Center )
	playerNumber:SetAnchorsAndPosition( 0, 0, 0.5, 0.5, 0, 0, _1080p * -7.5, _1080p * 6.5 )
	self:addElement( playerNumber )
	self.playerNumber = playerNumber
	
	local InstructionSwitch = nil
	
	InstructionSwitch = MenuBuilder.BuildRegisteredType( "vehicleInstruction", {
		controllerIndex = f1_local1
	} )
	InstructionSwitch.id = "InstructionSwitch"
	InstructionSwitch:SetRGBFromTable( SWATCHES.HUD.HUDMain, 0 )
	InstructionSwitch:SetAlpha( 0.75, 0 )
	InstructionSwitch:SetScale( -0.08, 0 )
	InstructionSwitch.Instruction:setText( Engine[0xED84C33EC5F01EA]( "KILLSTREAKS_HINTS/INSTRUCTION_SWITCH_SEATS" ), 0 )
	InstructionSwitch.Instruction:SetAlignment( LUI.Alignment.Center )
	InstructionSwitch:SetAnchorsAndPosition( 0, 1, 0, 1, _1080p * -90, _1080p * 110, _1080p * -2, _1080p * 22 )
	self:addElement( InstructionSwitch )
	self.InstructionSwitch = InstructionSwitch
	
	local f1_local7 = nil
	playerNumberBox:RegisterAnimationSequence( "DefaultSequence", {
		{
			value = 0,
			duration = 0,
			property = TWEEN_PROPERTY.Alpha
		}
	} )
	playerNumber:RegisterAnimationSequence( "DefaultSequence", {
		{
			value = nil,
			duration = 0,
			property = TWEEN_PROPERTY.Text
		}
	} )
	InstructionSwitch:RegisterAnimationSequence( "DefaultSequence", {
		{
			value = 0,
			duration = 0,
			property = TWEEN_PROPERTY.Alpha
		}
	} )
	self._sequences.DefaultSequence = function ()
		menu:AnimateSequence( "DefaultSequence" )
		menu:AnimateSequence( "DefaultSequence" )
		menu:AnimateSequence( "DefaultSequence" )
	end
	
	self._sequenceLengths.DefaultSequence = 0
	f0_local0( self, f1_local1, controller )
	ACTIONS.AnimateSequence( self, "DefaultSequence" )
	return self
end

MenuBuilder.registerType( "PlayerNumBox", PlayerNumBox )
LockTable( _M )
