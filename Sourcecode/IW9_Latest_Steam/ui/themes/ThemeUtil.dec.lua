-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 21-11-2025 08:48
THEMES = {
	themeTest = {}
}
THEMES.themeTest.text = {
	primaryText = COLORS.coralPink,
	secondaryText = COLORS.coralPink,
	buildNumber = COLORS.coralPink,
	dedicatedServer = COLORS.coralPink,
	overText = COLORS.coralPink,
	offwhite = COLORS.coralPink
}
THEMES.ResetTheme = function ()
	THEMES.ApplyTheme( THEMES.resetValues )
	THEMES.resetValues = {}
end

THEMES.ApplyTheme = function ( f2_arg0 )
	if not f2_arg0 or type( f2_arg0 ) ~= "table" then
		return 
	end
	THEMES.resetValues = THEMES.resetValues or {}
	for f2_local6, f2_local7 in pairs( f2_arg0 ) do
		THEMES.resetValues[f2_local6] = THEMES.resetValues[f2_local6] or {}
		for f2_local3, f2_local4 in pairs( f2_local7 ) do
			if not THEMES.resetValues[f2_local6][f2_local3] then
				THEMES.resetValues[f2_local6][f2_local3] = SWATCHES[f2_local6][f2_local3]
			end
			SWATCHES[f2_local6][f2_local3] = f2_local4
		end
	end
end

THEMES.ApplyDebugTheme = function ()
	for f1_local6, f1_local7 in pairs( SWATCHES ) do
		for f1_local3, f1_local4 in pairs( f1_local7 ) do
			SWATCHES[f1_local6][f1_local3] = COLORS.coralPink
		end
	end
end

