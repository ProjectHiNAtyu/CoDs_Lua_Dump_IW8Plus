-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 02-09-2025 10:42
module( ..., package.seeall )
function PlaylistList( menu, controller )
	local self = LUI.UIHorizontalStackedLayout.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 420 * _1080p, 0, 580 * _1080p )
	self.id = "PlaylistList"
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine.DDJFBBJAIG() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1 )
	local f1_local2 = self
	self:SetSpacing( 18 * _1080p )
	local Scrollbar = nil
	
	Scrollbar = MenuBuilder.BuildRegisteredType( "VerticalMinimalScrollbar", {
		controllerIndex = f1_local1
	} )
	Scrollbar.id = "Scrollbar"
	Scrollbar:SetAnchorsAndPosition( 0, 1, 0, 1, 0, _1080p * 16, 0, _1080p * 590 )
	self:addElement( Scrollbar )
	self.Scrollbar = Scrollbar
	
	local PlaylistButtons = nil
	
	PlaylistButtons = LUI.UIDataSourceGrid.new( {
		scrollingThresholdX = 1,
		buildArrowsLabel = true,
		maxVelocity = 5000,
		adjustSizeToContent = true,
		scrollingThresholdY = 1,
		wrapX = false,
		maxVisibleColumns = 1,
		maxVisibleRows = 12,
		wrapY = true,
		arrowsVerticalOffset = 0,
		springCoefficient = 400,
		controllerIndex = f1_local1,
		buildChild = function ()
			return MenuBuilder.BuildRegisteredType( "PlaylistButton", {
				controllerIndex = menu
			} )
		end,
		primaryAxis = LUI.DIRECTION.horizontal,
		spacingX = _1080p * 10,
		spacingY = _1080p * 10,
		columnWidth = _1080p * 420,
		rowHeight = _1080p * 40,
		horizontalAlignment = LUI.Alignment.Left,
		verticalAlignment = LUI.Alignment.Top
	} )
	PlaylistButtons.id = "PlaylistButtons"
	PlaylistButtons:setUseStencil( true )
	PlaylistButtons:SetAnchorsAndPosition( 0, 1, 0, 1, _1080p * 34, _1080p * 454, 0, _1080p * 590 )
	self:addElement( PlaylistButtons )
	self.PlaylistButtons = PlaylistButtons
	
	local f1_local5 = LUI.UIScrollbar.new
	local f1_local6 = {
		direction = LUI.DIRECTION.vertical,
		eventListener = Scrollbar,
		startCap = Scrollbar.startCap,
		endCap = Scrollbar.endCap,
		sliderArea = Scrollbar.sliderArea
	}
	local f1_local7 = Scrollbar.sliderArea
	if f1_local7 then
		f1_local7 = Scrollbar.sliderArea.slider
	end
	f1_local6.slider = f1_local7
	f1_local7 = Scrollbar.sliderArea
	if f1_local7 then
		f1_local7 = Scrollbar.sliderArea.fixedSizeSlider
	end
	f1_local6.fixedSizeSlider = f1_local7
	PlaylistButtons:AddScrollbar( f1_local5( f1_local6 ) )
	return self
end

MenuBuilder.registerType( "PlaylistList", PlaylistList )
LockTable( _M )
