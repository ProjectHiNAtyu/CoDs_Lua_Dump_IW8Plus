-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 04-10-2025 21:42
module( ..., package.seeall )
require( "ui.frontEnd.MPStoreBillboardButton" )
local f0_local0 = function ( f4_arg0, f4_arg1, f4_arg2 )
	f4_arg0.RowGrid:SetNumRows( 1 )
	f4_arg0.RowGrid:SetNumColumns( 0 )
end

function MPStoreBillboardGridRow( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 1680 * _1080p, 0, 190 * _1080p )
	self.id = "MPStoreBillboardGridRow"
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	f1_local1 = LUI.ValidateControllerIndex( self, f1_local1 )
	local f1_local2 = self
	local RowGrid = nil
	
	RowGrid = LUI.UIGrid.new( {
		maxVisibleRows = 1,
		maxVisibleColumns = 5,
		controllerIndex = f1_local1,
		buildChild = function ()
			return MenuBuilder.BuildRegisteredType( "MPStoreBillboardButton", {
				controllerIndex = menu
			} )
		end,
		refreshChild = function ( f2_arg0, f2_arg1, f2_arg2 )
			
		end,
		spacingX = _1080p * 10,
		spacingY = _1080p * 10,
		columnWidth = _1080p * 328,
		rowHeight = _1080p * 128
	} )
	RowGrid.id = "RowGrid"
	RowGrid:SetMaxVisibleColumns( 5 )
	RowGrid:SetRowSpacing( 10 )
	RowGrid:setUseStencil( false )
	RowGrid:SetAnchorsAndPosition( 0, 1, 0, 1, 0, _1080p * 1680, _1080p * 40, _1080p * 190 )
	self:addElement( RowGrid )
	self.RowGrid = RowGrid
	
	local CategoryText = nil
	
	CategoryText = LUI.UIStyledText.new()
	CategoryText.id = "CategoryText"
	CategoryText:SetRGBFromTable( SWATCHES.text.vTextOffWhite, 0 )
	CategoryText:setText( ToUpperCase( Engine[0xED84C33EC5F01EA]( "MENU/CATEGORIES" ) ), 0 )
	CategoryText:SetFont( FONTS.GetFont( FONT_STYLES.fontStyle_MHeaderBold.font.File ) )
	CategoryText:SetFontSize( FONT_STYLES.fontStyle_MHeaderBold.size )
	CategoryText:SetMinFontSize( FONT_STYLES.fontStyle_MHeaderBold.minSize )
	CategoryText:SetWordWrap( true )
	CategoryText:SetTracking( -2 * _1080p, 0 )
	CategoryText:SetAlignment( LUI.Alignment.Left )
	CategoryText:SetOptOutRightToLeftAlignmentFlip( true )
	CategoryText:SetMaxVisibleLines( 1 )
	CategoryText:SetAnchorsAndPosition( 0, 1, 0, 1, _1080p * 2, _1080p * 514, _1080p * 4, _1080p * 42 )
	self:addElement( CategoryText )
	self.CategoryText = CategoryText
	
	f0_local0( self, f1_local1, controller )
	return self
end

MenuBuilder.registerType( "MPStoreBillboardGridRow", MPStoreBillboardGridRow )
LockTable( _M )
