-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 04-10-2025 21:43
module( ..., package.seeall )
local f0_local0 = function ( f4_arg0, f4_arg1, f4_arg2 )
	local f4_local0 = false
	
	local bindButton = LUI.UIBindButton.new()
	bindButton.id = "selfBindButton"
	f4_arg0:addElement( bindButton )
	f4_arg0.bindButton = bindButton
	
	if IsLanguageArabic() then
		ACTIONS.AnimateSequence( f4_arg0, "AR" )
	end
end

function SwapTriggersPrompt( menu, controller )
	local self = LUI.UIElement.new()
	self:SetAnchorsAndPosition( 0, 1, 0, 1, 0, 500 * _1080p, 0, 34 * _1080p )
	self.id = "SwapTriggersPrompt"
	self._animationSets = self._animationSets or {}
	self._sequences = self._sequences or {}
	self._sequenceLengths = self._sequenceLengths or {}
	local f1_local1
	if controller then
		f1_local1 = controller
	else
		f1_local1 = controller.controllerIndex
	end
	if not f1_local1 and not Engine[0x12586A3F4FB490E]() then
		f1_local1 = self:getRootController()
	end
	assert( f1_local1, "Missing controllerIndex!" )
	local f1_local2 = self
	local Background = nil
	
	Background = LUI.UIImage.new()
	Background.id = "Background"
	Background:SetRGBFromInt( 0, 0 )
	Background:SetAlpha( 0.15, 0 )
	self:addElement( Background )
	self.Background = Background
	
	local SwapTriggers = nil
	
	SwapTriggers = LUI.UIText.new()
	SwapTriggers.id = "SwapTriggers"
	SwapTriggers:SetRGBFromTable( SWATCHES.genericButton.description, 0 )
	SwapTriggers:setText( Engine[0xED84C33EC5F01EA]( "MENU/FLIP_TRIGGERS_WITH_BUMPERS_PROMPT" ), 0 )
	SwapTriggers:SetFont( FONTS.GetFont( FONTS.MainRegularHeader.File ) )
	SwapTriggers:SetFontSize( 22 * _PT )
	SwapTriggers:SetMinFontSize( 16 * _PT )
	SwapTriggers:SetAlignment( LUI.Alignment.Right )
	SwapTriggers:SetOptOutRightToLeftAlignmentFlip( true )
	SwapTriggers:SetAnchorsAndPosition( 0, 0, 0.5, 0.5, _1080p * 25, _1080p * -15, _1080p * -10, _1080p * 12 )
	self:addElement( SwapTriggers )
	self.SwapTriggers = SwapTriggers
	
	local Line = nil
	
	Line = LUI.UIImage.new()
	Line.id = "Line"
	Line:SetAlpha( 0.25, 0 )
	Line:SetAnchorsAndPosition( 0, 0, 1, 0, _1080p * 15, _1080p * -15, 0, _1080p * 1 )
	self:addElement( Line )
	self.Line = Line
	
	self._sequences.DefaultSequence = function ()
		
	end
	
	self._sequenceLengths.DefaultSequence = 0
	local f1_local6 = nil
	SwapTriggers:RegisterAnimationSequence( "AR", {
		{
			value = 22,
			duration = 0,
			property = TWEEN_PROPERTY.Text_Size
		},
		{
			duration = 0,
			property = TWEEN_PROPERTY.Bottom,
			value = _1080p * 16
		},
		{
			duration = 0,
			property = TWEEN_PROPERTY.Left,
			value = _1080p * 25
		},
		{
			duration = 0,
			property = TWEEN_PROPERTY.Top,
			value = _1080p * -14
		},
		{
			duration = 0,
			property = TWEEN_PROPERTY.Right,
			value = _1080p * -15
		},
		{
			duration = 0,
			property = TWEEN_PROPERTY.Text_Style_Horizontal_Alignment,
			value = LUI.Alignment.Left
		}
	} )
	self._sequences.AR = function ()
		menu:AnimateSequence( "AR" )
	end
	
	self._sequenceLengths.AR = 0
	f0_local0( self, f1_local1, controller )
	return self
end

MenuBuilder.registerType( "SwapTriggersPrompt", SwapTriggersPrompt )
LockTable( _M )
