-- Generated by CoDLuaDecompiler
-- Version: 2.4.2
-- Date: 13-07-2025 21:51
module( ..., package.seeall )
CoD.PrintModuleLoad( _NAME )
local f0_local0 = 1.2
function invalidCmdHintDef()
	local self = LUI.UIElement.new()
	self.id = "invalidCmdHintId"
	self:setupOwnerdraw( CoD.Ownerdraw.CGInvalidCmdHint, f0_local0, CoD.TextStyle.Shadowed )
	self:SetAnchorsAndPosition( 0.5, 0.5, 0, 1, -32, 32, 162, 226 )
	self:SetAlpha( 0.6 )
	self:SetFont( CoD.TextSettings.HudEuroMedSmallFont.Font )
	self:SetAlignment( LUI.Alignment.Center )
	return self
end

function mountHintDef()
	local self = LUI.UIElement.new()
	self.id = "mountHintId"
	self:setupOwnerdraw( CoD.Ownerdraw.CGMountHint, f0_local0, CoD.TextStyle.Shadowed )
	if CONDITIONS.IsSplitscreen() then
		self:SetAnchorsAndPosition( 0.5, 0.5, 1, 0, -32, 32, -120, -96 )
	else
		self:SetAnchorsAndPosition( 0.5, 0.5, 1, 0, -32, 32, -148, -124 )
	end
	self:SetAlpha( 0.6 )
	self:SetFont( FONTS.GetFont( FONTS.MainRegular.File ) )
	self:SetAlignment( LUI.Alignment.Right )
	return self
end

MenuBuilder.registerType( "Hints", function ()
	local self = LUI.UIElement.new()
	self.id = "Hints"
	local f1_local1 = false
	if Engine.ECHCFGDDDF() and GameX.IsRealismMode() then
		return self
	else
		self:addElement( invalidCmdHintDef() )
		self:addElement( mountHintDef() )
		return self
	end
end )
LockTable( _M )
